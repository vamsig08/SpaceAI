# Development Rules

Never use TODO placeholders.

Never use mock implementations unless explicitly requested.

Never skip tests.

Never skip error handling.

Never leave incomplete functions.

Always create production-quality implementations.

Always explain design decisions.

Always update documentation after implementation.

Always verify imports and dependencies.

Always generate runnable code.

Always provide commands required to run and test changes.